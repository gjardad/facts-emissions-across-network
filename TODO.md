# TODO

1. Regenerate `deployment_proxy_list.RData` on RMD with `B_DRAWS = 200` (current local copy has B = 50) and copy back to local 1, so the GLO allocation pipeline runs against the full set of EN repeats.
2. Check how precise the pre-ETS backcasting is — currently `b_allocation_glo.R` reports group-years where `E_ETS + E_pre_ets > E_NIR` (floored to 0). This shouldn't happen and indicates the backcast is over-attributing emissions to pre-ETS years for some CRF groups. Investigate which CRF groups / years are affected and whether the sector-year FE + first-two-year anchor method needs to be revised. Two specific things to do:
   - **Validate on always-ETS firms.** Take firms observed in EU ETS for the entire 2005–2021 window, hide their first few years (e.g. pretend they entered in 2008 or 2012), backcast the hidden years using the deployed sector-year FE + first-two-year anchor procedure, and compare backcasted vs. actual. Report the typical magnitude and sign of the error, by CRF group.
   - **Address the year-window methodological change in `b_allocation_glo.R`.** The original backcast restricted pre-ETS years to those in which the firm appeared in `annual_accounts_selected_sample_key_variables.RData`. The new GLO allocation script uses `seq(min(YEARS), T_entry − 1L)` instead, which assumes the firm exists for every year prior to its ETS entry. This was a deliberate change to remove the dependency on the downsampled annual accounts file on local 1, but it can over-backcast for firms that did not actually exist pre-2005 or that had a later founding date. Decide whether to keep this convention, restore the original (with a full-coverage firm-year existence source), or build a separate full-coverage existence lookup.
3. Introduce an uncertainty propagation pipeline for scope 1 emissions on top of the GLO allocation: produce per-firm-year prediction intervals (e.g. 95%) that reflect the uncertainty in the extensive margin (who is truly an emitter, calibrated from `p_i`) and the intensive margin (where in the within-cell distribution they fall, calibrated from `proxy_mean_i`). See `IMPUTATION_USING_SUMMARY_ACROSS_REPEATS.md` and `~/Downloads/deployment_prediction_interval_step_by_step.md` for the framework.

4. **Update `inferring_emissions/` extensive margin classifier from `p_i ≥ τ` to logistic on `(p_i, log(1+proxy_mean))`.** The OOS validation in `IMPUTATION_USING_SUMMARY_ACROSS_REPEATS.md` currently classifies imputed emitters using a single threshold on `p_i`. Switching to a LOSO-fit logistic on both `p_i` and `log(1+proxy_mean)` improves held-out PPV from ~0.43 to ~0.56 with TPR essentially preserved (LOSO results computed in `analysis/tmp_loso_logistic.R` and `analysis/tmp_ppv_check.R`). The threshold `q*` should be cross-validated by maximizing Youden's J on the training fold inside each LOSO. Document explicitly that the *choice* of feature set is based on LOSO comparison across only three mixed CRFs and is therefore mildly optimistic — this is a tuning choice we cannot fully cross-validate. The deployed pipeline (`b_allocation_glo.R`) and the uncertainty pipeline (`uncertainty_propagation.R`) must use the same classifier as the one validated in `inferring_emissions/`.
